import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  button: {
    padding: 10,
    backgroundColor: "#014898",
    borderRadius: 5,
  },
  buttonText: {
    color: "#fff",
    textAlign: "center",
  },
});
