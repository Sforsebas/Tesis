import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  btnContainer: {
    marginBottom: 20,
  },
  btn: {
    backgroundColor: "#014898", // El color que quieres usar
  },
});
